{{> header }}

{{{ imports }}}

declare module '@chainx-v2/rpc-core/types.jsonrpc' {
export interface RpcInterface {
{{#each modules}}
{{{name}}}: {
{{#each items}}
{{> docs}}
{{{name}}}: AugmentedRpc<{{#if generic}}<{{{generic}}}>{{/if}}({{{args}}}) => Observable<{{{type}}}>>;
    {{/each}}
    };
    {{/each}}
    }
    }
    {{> footer }}