import { WsProvider } from '@chainx-v2/rpc-provider';
import ApiPromise from '../promise';
import Slake from './XStaking';
import Asset from './Asset';
import GateWayCommon from './GateWayCommon';
import { Account } from '@chainx-v2/account';
declare class ChainX {
    private _provider;
    private _api;
    private _stake;
    private _asset;
    private _gatewaycommon;
    constructor(wsUrlOrProvider?: string);
    getProvider(): WsProvider;
    setProvider(wsUrlOrProvider?: string): void;
    ready(): Promise<void>;
    get account(): Account;
    get asset(): Asset;
    get gatewaycommon(): GateWayCommon;
    get slake(): Slake;
    get api(): Promise<ApiPromise>;
    get provider(): WsProvider;
}
export default ChainX;
