import { Header, Index } from '@chainx-v2/types/interfaces';
import { AnyNumber, Codec, IExtrinsicEra } from '@chainx-v2/types/types';
import { Observable } from 'rxjs';
import { ApiInterfaceRx } from '@chainx-v2/api/types';
interface Result {
    header: Header | null;
    mortalLength: number;
    nonce: Index;
}
export declare function signingInfo(api: ApiInterfaceRx): (address: string, nonce?: AnyNumber | Codec, era?: IExtrinsicEra | number) => Observable<Result>;
export {};
