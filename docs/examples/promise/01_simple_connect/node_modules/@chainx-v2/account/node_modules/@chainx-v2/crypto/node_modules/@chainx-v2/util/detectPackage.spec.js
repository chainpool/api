"use strict";

var _ = require(".");

// Copyright 2017-2020 @chainx-v2/util authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
describe('assertSingletonPackage', () => {
  const PKG = '@chainx-v2/util';
  const VER1 = '9.8.0-beta.45';
  const VER2 = '9.7.1';
  const PATH = '/Users/jaco/Projects/polkadot-js/api/node_modules/@chainx-v2/util';
  it('should not log the first time', () => {
    const spy = jest.spyOn(console, 'warn');
    (0, _.detectPackage)({
      name: PKG,
      version: VER1
    }, `${PATH}/01`);
    expect(spy).not.toHaveBeenCalled();
    spy.mockRestore();
  });
  it('should log the second time', () => {
    const spy = jest.spyOn(console, 'warn');
    (0, _.detectPackage)({
      name: PKG,
      version: VER2
    }, `${PATH}/02`);
    expect(spy).toHaveBeenCalledWith(`Multiple instances of @chainx-v2/util detected, ensure that there is only one package in your dependency tree.\n\t${VER1}\t${PATH}/01\n\t${VER2}        \t${PATH}/02`);
    spy.mockRestore();
  });
});