import { ApiInterfaceRx } from '@chainx-v2/api/types';
import { AccountId, Address } from '@chainx-v2/types/interfaces';
import { DeriveAccountFlags } from '../types';
import { Observable } from 'rxjs';
/**
 * @name info
 * @description Returns account membership flags
 */
export declare function flags(api: ApiInterfaceRx): (address?: AccountId | Address | string | null) => Observable<DeriveAccountFlags>;
